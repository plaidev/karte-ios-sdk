name: "CHANGELOG Lint"
on:
  pull_request:
    branches:
      - 'develop'
      - 'master'
    paths:
      - 'CHANGELOG.md'
permissions:
  id-token: write
  contents: read
  pull-requests: write
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  changelog-lint:
    if: github.repository == 'plaidev/karte-ios-dev'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false
      - uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        id: app-token
        with:
          app-id: ${{ secrets.APP_TEAM_GITHUB_APP_ID }}
          private-key: ${{ secrets.APP_TEAM_GITHUB_APP_PRIVATE_KEY }}
          owner: plaidev
          repositories: ${{ vars.CLAUDE_PLUGIN_REPO_NAME }}
          permission-contents: read
      - name: Clone changelog-lint plugin
        env:
          APP_TOKEN: ${{ steps.app-token.outputs.token }}
          CLAUDE_PLUGIN_REPO_NAME: ${{ vars.CLAUDE_PLUGIN_REPO_NAME }}
        run: |
          git clone --depth=1 "https://x-access-token:${APP_TOKEN}@github.com/plaidev/${CLAUDE_PLUGIN_REPO_NAME}.git" /tmp/plugin-repo
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        id: auth_gcp
        with:
          workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}
          token_format: 'id_token'
          id_token_audience: ${{ vars.SHARED_LITELLM_PROXY_URL }}
          id_token_include_email: true
      - name: Mask id_token
        run: echo "::add-mask::${{ steps.auth_gcp.outputs.id_token }}"
      - uses: anthropics/claude-code-action@9dd8b95a392eb34b6f5fb56cf5a64cb735912d4b # v1.0.150
        env:
          ANTHROPIC_BASE_URL: ${{ vars.SHARED_LITELLM_PROXY_URL }}
        with:
          github_token: ${{ github.token }}
          allowed_bots: "app-team-github-app[bot]"
          # WIF の id_token を Authorization: Bearer で送る（litellm の JWT 認証用）
          claude_code_oauth_token: ${{ steps.auth_gcp.outputs.id_token }}
          claude_args: |
            --plugin-dir /tmp/plugin-repo/plugins/changelog-lint
            --allowedTools 'Read,Write'
            --model claude-sonnet-4-6
          prompt: |
            changelog-lintスキルを使ってCHANGELOG.mdをチェックしてください。
            チェック結果を /tmp/changelog_lint_result.txt に書き出してください。
      - name: Check lint result
        run: |
          if [ ! -f /tmp/changelog_lint_result.txt ]; then
            echo "❌ Changelog lint result file not found."
            exit 1
          fi
          cat /tmp/changelog_lint_result.txt
          if grep -q "❌" /tmp/changelog_lint_result.txt; then
            exit 1
          fi
      - name: Post lint errors as PR comment
        if: failure()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ ! -f /tmp/changelog_lint_result.txt ] || ! grep -q "❌" /tmp/changelog_lint_result.txt; then
            exit 0
          fi
          {
            echo "## ❌ CHANGELOG Lint エラー"
            echo ""
            cat /tmp/changelog_lint_result.txt
          } | gh pr comment "${{ github.event.pull_request.number }}" \
            --repo "${{ github.repository }}" \
            --body-file -
